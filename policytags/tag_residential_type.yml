version: 2
models:
  - name: tag_residential_type
    schema: bqd_995_marts
    config:
      alias: RESIDENTIAL_TYPE
    columns:
    - name: Residential_Type_Code
      policy_tags:
        - '{{ var("DEFAULT_PT_TAXONOMY") ~ var("DEFAULT_PT_INTERNAL") }}'
    - name: Residential_Type_Desc
      policy_tags:
        - '{{ var("DEFAULT_PT_TAXONOMY") ~ var("DEFAULT_PT_INTERNAL") }}'